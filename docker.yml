- hosts: all
  tasks:
  - name: install docker
    package:
      name: "docker"
      state: latest
  - name: ensure docker is installed
    service:
      name: docker
      state: started
      enabled: yes
